/* Write your T-SQL query statement below */
select employee_id
from employees a
where salary < 30000 and manager_id is not null
and
not exists (select employee_id from employees where a.manager_id = employee_id)
order by employee_id